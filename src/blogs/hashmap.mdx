# Modern Hashmaps

Recently, I've been looking into how high performance hashmaps work.
As part of that exploration, I've tried to understand the state of the art.
In this post, we'll look at how Swiss Tables compare to Boost Hashmaps, and my own experimental new hashmap design.

I tried implementing two possibly novel ideas:

- A compressed Robin Hood hashmap using the rank select quotient filter bitmap.
- A SIMD Cuckoo hashmap with fingerprints inspired by swiss tables.

While the first one ended up being much slower than I would like, the SIMD Cuckoo hashmap ended up being very fast, and is a promising design for a future production ready hashmap.

I've chosen to code in Zig because its focus on performance and low-level control makes it easier to design high-performance data structures.
Additionally, the current Zig hashmap implementation has some performance [issues](https://github.com/ziglang/zig/issues/17851) that we'll try to improve upon.

We're going to look here at a hashmap with the following simple API:

```zig
fn compare(a: KeyType, b: KeyType) bool { ... }
fn hash(key: KeyType) u64 { ... }

const HashMap = struct {
    pub fn init(allocator: *std.mem.Allocator) HashMap { ... }
    pub fn insert(self: *HashMap, key: KeyType, value: u64) void { ... }
    pub fn get(self: *HashMap, key: KeyType) ?u64 { ... }
    pub fn remove(self: *HashMap, key: KeyType) void { ... }
}
```

To use this HashMap, we use 64 bit values because they pack nicely, and can represent an arbitrary pointer to memory.

Modern hashmaps tend to use open addressing due to the better cache locality. Notice the large number of pointers below

<InteractiveChain />

This hurts cache locality, because as we traverse each chain, we have to jump around memory.
Additionally, each node has to store a pointer to the next node, which takes up space.

# Robin Hood Hashmaps

One slight variant of the standard hashmap is to include the distance from the ideal bucket in the hash table, and to use that distance to determine which key to evict when a collision occurs.
This is called Robin Hood Hashing, and it has the advantage of reducing the variance in lookup times, because it tries to keep all keys as close to their ideal home as possible.
One additional trick is that robin hood hashing can avoid tombstones, because when a key is removed, we know which keys belong to which runs and can therefore shift them back into the correct location.

<RobinHoodPslInsert />

The top row in the bitmap is the homes, which represent wether any key was intended for that hashmap slot. Hover a Homes bit to light its PSL run. The highlighted PSL cell is the current insertion target.

Note we actually define $homes_i = \exists j : h(key_j) \\% n = i,$ meaning that the home bit is set if there is a run that starts at or before this slot.
During `get`, we can use this bitmap to quickly skip over empty slots.

# Rank Select Quotient Filters

In class, we learned about rank and select quotient filters, which use two bitmap to store run starts and run ends.

Based on the above, I tried creating a compressed Robin Hood hashmap using the RSQF bitmap.

I tried implementing this in two ways. One using the buckets with offsets shown below, and a second time using a prefix sum tree to dynamically calculate the offsets.

This reduced the data requirement nicely, however this left the performance suboptimal (see the graphs below), because the operations to find runs are still pretty heavy, and because of a core missing trick: SIMD buckets. (This would be a good target for a future project, to try and merge SIMD bucketing with the RSQF hashmap idea).

<RSQFInsert />

Here, the top row shows the run "homes" and the bottom row shows the run end bitmap. The offsets show how far in another run has intruded into this block.
Just like above, you can hover over a Homes bit to light its PSL run.


## The Problem Of Clustering

<ProbingDemo />

As the diagrams run, notice how the average chain length of linear probing is noticeably higher than triangular probing.

This is caused by [clustering](https://en.wikipedia.org/wiki/Primary_clustering), because the longer the chain, the more likely it is that new keys will hash to the same index, causing the chain to grow even longer.

Even though quadratic probing reduces clustering, it still has some locality preference, which leads to similar clustering.

We should be able to avoid this with randomized probing, but that would be expensive (both to hash multiple times, and because of the unpredictable memory accesses).

Note that despite this slowdown, we might still choose locality and linear probing because of our ability to use SIMD instructions (see below).

# Swiss Tables

Swiss Tables are a modern SIMD friendly variation on quadratic probing with tombstones.
They store large buckets of 16 entries, and use a 7 bit fingerprint to determine if a key is present in the bucket.
Because SIMD instructions can operate on 16 bytes at a time, this allows for very fast lookups a bucket at a time (which drastically reduces the number of buckets that need to be searched).

```zig
const tags: @Vector(16, u8) = self.loadTagGroup(bucket);

const tag_lane: @Vector(16, u8) = @splat(tag);
const matches: @Vector(16, u8) = tags == tag_lane;
```

<SwissSplatCompare />

# Cuckoo Hashing

We can take the bucketing idea of Swiss tables and combine it with Cuckoo filters (but this time store actual data) to make a SIMD Cuckoo hashtable with fingerprints.
Unlike the original Cuckoo hashing paper, we store fingerprints so that we can do SIMD bucket checks.

In essence, Cuckoo hashing moves the work from the lookup phase to the insertion phase, where we must find careful buckets for the key.

Unsurprisingly, because Cuckoo filters require only two SIMD block lookups for each key, they have extremely fast lookups (constant time).
Similarly, they have extremely fast deletions, because we can just remove the key from the bucket.

They push quite a bit more work to inserts, where they are forced to move keys around to make room, leading to potentially long "kick" chains.

<CuckooInsert />

One thing to note is that all the fast designs actually use this cuckoo bucketing idea, where we can compare and insert into anywhere in an entire bucket.

# Graphs

I designed a series of benchmarks to compare the performance of different hashmap implementations.
In these benchmarks, I focused on frequent insertions, lookups, and deletions (but ignored iteration time).
I tested with both keys and values being 64 bit integers, and using Wyhash as the hash function.

`delete_churn` keeps a fixed live set: each operation removes one currently live key from a rotating slot and inserts a new key, so occupancy stays constant while deletion handling is continuously stressed.
`alternate_reuse` also keeps occupancy constant, but it repeatedly deletes and reinserts the same key universe (`key = i % working_set`), which minimizes key-space growth and isolates the cost of repeated remove/put on reused slots.
The load benchmarks pre-size a table to a target capacity, choose an item count at a specific load fraction, and then test three behaviors: `lookup_hit` cycles through existing keys, `lookup_miss` probes absent keys beyond the inserted range, and `insert_load` measures pure insertion throughput from empty to the target count.
Together, they separate mutation-heavy steady-state behavior (churn/reuse) from load-factor-sensitive read/write behavior to identify causes of degredation.

<HashmapConfigBenchmarks />

<HashmapLoadBenchmarks />

# Links

- Swiss Tables C++: https://abseil.io/about/design/swisstables
- Swiss Tables Go: https://go.dev/blog/swisstable
- Boost Hashmaps: https://bannalia.blogspot.com/2022/11/inside-boostunorderedflatmap.html
- RSQF: https://dl.acm.org/doi/pdf/10.1145/3035918.3035963
- Rust RSQF: https://github.com/arthurprs/qfilter
- Bucketed Cuckoo Hashing: https://www.di.ens.fr/~vergnaud/algo0910/Cuckoo.pdf
